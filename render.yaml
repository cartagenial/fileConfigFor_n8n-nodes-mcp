services:
  - type: web
    plan: free
    name: my_n8n-mcp
    env: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: N8N_COMMUNITY_PACKAGES_ALLOW_TOOL_USAGE
        value: "true"
      - key: N8N_PORT
        value: "5678"
